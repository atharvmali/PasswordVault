# PasswordVault
A secure, lightweight Password Vault desktop application built in Java Swing with MySQL for persistent storage. It features encrypted password saving, master-password authentication, auto-lock, and a user-friendly UI.
